version: "5"
services:
  uploadservice:
    build: .
    restart: always
    container_name: ${UPLOAD_CONTAINER_NAME}
    networks:
      - local-diskie-net
networks:
  local-diskie-net:
    external: true

#todo add volume
