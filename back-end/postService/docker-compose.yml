version: "5"
services:
  postservice:
    build: .
    restart: always
    container_name: ${POST_CONTAINER_NAME}
    networks:
      - local-diskie-net
networks:
  local-diskie-net:
    external: true

#todo add volume
